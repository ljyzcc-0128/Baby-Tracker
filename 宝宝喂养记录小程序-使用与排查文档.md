

---

# 宝宝喂养记录小程序 —— 使用与排查文档

## 一、项目简介

这是一个用于记录宝宝日常喂养、排泄、睡眠等信息的微信小程序，无需后端服务器，所有数据保存在手机本地（微信小程序的本地存储）。

### 已实现功能
- 一键记录：小便、大便、母乳、奶瓶（可输入毫升数）、喂水、喂AD、喂D3、辅食、睡眠（点两下自动计时）、洗澡、体温（可输入度数）、换尿布、用药、自定义类型
- 每个记录按钮显示"距上次 X 小时前"
- 今日记录时间轴，长按可删除
- 近 7 天统计柱状图（各类型记录次数趋势）
- 多宝宝管理与快速切换（适合双胞胎/二胎家庭）

### 建议的后续迭代方向（概览）
1. **定时提醒**：喂AD/D3、喂奶间隔提醒
2. **生长曲线**：身高体重头围记录，对比 WHO 标准曲线
3. **疫苗接种日历**：内置免疫规划时间表 + 提前提醒
4. **云端同步/家庭共享**：微信云开发，多人共同记录
5. **数据导出**：生成 PDF 或长图，给医生看
6. **夜间模式**：夜奶时降低屏幕亮度和对比度

详细的可执行任务拆解见「六、开发事项清单」，扩展功能方案见「七、功能扩展方案」。

---

## 二、项目文件结构

baby-tracker/
├── app.js                  # 小程序入口逻辑，初始化本地存储
├── app.json                # 全局配置（页面列表、导航栏样式）
├── app.wxss                # 全局样式
├── project.config.json     # 项目配置（AppID、编译类型等）
├── sitemap.json            # 小程序索引配置
└── pages/
    ├── index/     # 首页：一键打点 + 今日时间轴
    ├── stats/     # 统计页：近7天各类型次数柱状图
    └── profile/   # 宝宝管理：多宝宝切换、新增

```

---

## 三、安装与运行步骤

1. 下载并解压项目压缩包，得到 `baby-tracker` 文件夹
2. 下载安装「微信开发者工具」（稳定版）：
   https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
3. 打开工具，用微信扫码登录（普通个人微信号即可）
4. 「导入项目」→ 目录选择 `baby-tracker` 文件夹 → AppID 选择「测试号」或「游客模式」
5. 点击导入，工具自动编译，左侧模拟器即可预览；右上角「预览」可生成二维码用手机扫码体验

> 若之后想发布给家人正式使用，需要去 https://mp.weixin.qq.com 注册正式小程序账号，换上正式 AppID 提交审核。

---

## 四、常见问题排查（踩坑记录）

下面是实际调试过程中遇到的问题和对应解决办法，按优先级从高到低排查：

### 1. 控制台报错 `webapi_getwxaasyncsecinfo:fail appid missing`
**原因**：项目 AppID 为空。
**解决**：顶部菜单「详情」→ 确认 AppID 不为空，选择「测试号」或「游客模式」。

### 2. 提示 `Page "pages/index/index" has not been registered yet.`
**原因**：通常是以下几种情况之一：
- `app.json` 里的 `pages` 数组没有正确列出该页面
- `project.config.json` 的 `miniprogramRoot` 设置和实际项目结构不一致（比如误设为 `miniprogram/` 子目录）
- 对应页面的 `.js` 文件内容有误（比如误粘贴成了其他格式的内容）、或文件被误删

**解决**：
1. 检查 `app.json` 中 `pages` 数组是否包含 `"pages/index/index"`
2. 检查 `project.config.json` 是否有 `miniprogramRoot` 字段，若有且不为空，改成空字符串或删除该行
3. 检查 `pages/index/index.js` 文件内容是否是正确的 JS 代码（以 `Page({` 开头），而不是被误粘贴成了 JSON 内容
4. 修改后需要**完全退出开发者工具**（不只是关闭项目窗口）再重新打开、重新导入

### 3. `sitemap.json` 缺失导致的警告
**解决**：在项目根目录新建 `sitemap.json`，内容：
```json
{
  "rules": [{
    "action": "allow",
    "page": "*"
  }]
}
```

### 4. `project.config.json` 内容不完整（缺少 `compileType` 等字段）
**现象**：文件里只有一两行内容（比如只有 `appid` 和 `isGameTourist`），说明项目类型可能没有被正确识别为「小程序」。
**解决**：用完整内容替换：
```json
{
  "description": "项目配置文件",
  "packOptions": { "ignore": [], "include": [] },
  "setting": {
    "urlCheck": false, "es6": true, "postcss": true, "minified": true,
    "newFeature": true, "coverView": true, "nodeModules": false,
    "autoAudits": false, "checkInvalidKey": true, "checkSiteMap": true,
    "uploadWithSourceMap": true, "useIsolateContext": true,
    "useCompilerModule": true, "minifyWXSS": true
  },
  "compileType": "miniprogram",
  "libVersion": "",
  "appid": "你的AppID",
  "projectname": "baby-tracker",
  "simulatorType": "wechat",
  "simulatorPluginLibVersion": {},
  "condition": {}
}
```

### 5. 使用「灰度/beta 基础库」导致的不稳定报错
**现象**：控制台提示"正在使用灰度中的基础库 x.x.x 进行调试"，伴随各种奇怪的脚本报错（如 `appServiceSDKScriptError`）。
**解决**：顶部「详情」→「本地设置」→「调试基础库」，切换成带「稳定版」标识的版本。

### 6. `SystemError (appServiceSDKScriptError) timeout`
**原因**：开发者工具请求微信服务器超时，属于网络连接问题，与代码无关。
**排查顺序**：
1. 关闭电脑上的 VPN / 代理软件
2. 确认浏览器能正常访问外网
3. 检查防火墙/安全软件是否拦截了开发者工具的网络请求
4. 确认网络正常后，完全退出并重新打开开发者工具

### 7. 排查思路总结
遇到空白页面时，按以下顺序检查最快定位问题：
1. 看「调试器」→「Console」标签页的红色报错信息（不是警告）
2. 看「构建」标签页的编译日志，确认所有文件编译成功、没有语法错误
3. 检查 `app.json`、`project.config.json` 内容是否完整正确
4. 检查对应页面的 `.js` / `.wxml` / `.wxss` 三个文件是否都存在且内容正确
5. 排除网络/基础库版本等环境因素（换稳定版基础库、关闭 VPN）
6. 实在无法定位时，用未经手动修改的原始项目文件重新导入一次，排除人为改错的干扰

---

## 五、遇到问题时可以求助的地方

- 微信开发者社区（有大量类似报错的讨论）：https://developers.weixin.qq.com/community/develop/mixflow
- 微信小程序官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/

---

## 六、开发事项清单（Backlog）

按优先级从高到低排列，每项列出所需技术方案、涉及文件/页面、大致工作量。可以直接当作开发任务清单勾选使用。

### P0（建议优先做，实用性最高）

- [ ] **定时提醒（喂AD/D3、喂奶间隔）**
  - 技术方案：`wx.requestSubscribeMessage` 一次性订阅消息，需在小程序后台申请消息模板
  - 涉及：新增「提醒设置」页面（`pages/reminder/`），`index.js` 里判断距上次时间超过阈值时提示订阅
  - 前置条件：需要小程序正式账号（订阅消息不支持测试号）
  - 工作量：中（主要卡在申请消息模板的审核流程）

- [ ] **生长曲线记录**
  - 技术方案：新增数据类型（身高/体重/头围），存储结构 `growthRecords: [{date, height, weight, headCrc}]`
  - 涉及：新增「生长记录」页面（`pages/growth/`），用 `canvas` 或简单折线图绘制曲线，可选内置 WHO 标准曲线数据做背景参考线
  - 工作量：中高（图表绘制部分需要额外开发或引入图表库）

- [ ] **数据导出（生成长图/PDF）**
  - 技术方案：用 `wx.canvasToTempFilePath` 把统计页面绘制成图片，再 `wx.saveImageToPhotosAlbum` 保存到相册
  - 涉及：`stats.js` 增加导出按钮和绘制逻辑
  - 工作量：中

### P1（体验优化类）

- [ ] **疫苗接种日历**
  - 技术方案：内置国家免疫规划时间表（按宝宝出生日期自动计算各针次时间），本地存储接种状态
  - 涉及：新增「疫苗」页面（`pages/vaccine/`）
  - 工作量：中（主要是时间表数据整理）

- [ ] **夜间模式**
  - 技术方案：监听系统深色模式或提供手动开关，切换 `page` 背景色和文字颜色的 CSS 变量
  - 涉及：`app.wxss` 增加深色主题样式，各页面 `.wxss` 适配
  - 工作量：低

- [ ] **语音快速记录**
  - 技术方案：微信同声传译插件或 `wx.getRecorderManager` + 语音识别接口，解析关键词匹配记录类型
  - 涉及：`index.js` 增加语音按钮和识别回调
  - 工作量：高（依赖第三方语音识别能力，准确率需要测试）

### P2（需要云开发支持，改动较大）

- [ ] **云端同步/家庭共享**
  - 技术方案：接入微信云开发（云数据库替代 `wx.setStorageSync`），通过邀请码或群二维码让家庭成员加入同一个宝宝的数据空间
  - 涉及：几乎所有页面的数据读写逻辑都要从本地存储改造成云数据库调用；需要设计数据库表结构（babies、records、family_members 三张表）和权限规则
  - 工作量：高（属于架构级改动，建议单独排期）

- [ ] **多端提醒/小组件**
  - 技术方案：依赖云开发 + 定时触发器（云函数定时任务）主动推送提醒，而不是被动等用户打开小程序触发订阅
  - 前置条件：需先完成「云端同步」这一项
  - 工作量：高

---

## 七、功能扩展方案（新增）

以下是从真实带娃场景出发，在原 Backlog 之外补充的功能方案。按「是否需要正式账号/云开发」和「技术难度」标注，方便判断优先级。

### 7.1 喂奶场景深度扩展

**（1）左右侧哺乳切换与计时（母乳喂养专用）**
- **痛点**：母乳妈妈需要记录"左边喂了15分钟，换右边再喂10分钟"，还得记住上次是哪边先喂的，下次要换边。
- **功能设计**：点"母乳"按钮时弹出左右侧选择，自动计时，结束时保存"左15min/右10min"。首页显示"上次先喂：左侧，下次建议先喂：右侧"。
- **技术**：纯本地存储，新增数据结构 `{type:'breast', side:'left', duration:900}`。难度**低**。

**（2）奶瓶"剩余量"记录**
- **痛点**：冲了120ml，宝宝只喝了80ml，剩下40ml倒了。现在只能记"奶瓶120ml"，实际摄入量不准。
- **功能设计**：输入"冲调量"和"剩余量"，自动算出实际摄入量。统计页按"实际摄入"算，不按冲调量。
- **技术**：表单加两个输入框，统计逻辑改一下。难度**低**。

**（3）亲喂 + 瓶喂混合统计**
- **痛点**：母乳亲喂没法量化毫升数，但统计时又想看"今天大概吃了多少"。可以给亲喂一个"估算时长→估算奶量"的换算系数（比如1分钟≈10ml），让用户自己设。
- **技术**：设置页加一个换算系数，统计时统一折算。难度**低**。

### 7.2 睡眠场景深度扩展

**（4）睡眠质量标记**
- **痛点**：只记了"睡了2小时"，但不知道睡得安不安稳（有没有惊醒、哭闹）。
- **功能设计**：睡眠结束后，让用户点一下"安稳/一般/频繁醒"，存一个字段。统计页可以看"本周安稳睡眠占比"。
- **技术**：本地存储加字段，统计页加一个百分比展示。难度**低**。

**（5）接觉提醒**
- **痛点**：宝宝睡30-45分钟容易醒，需要提前去接觉，但家长经常忘记看时间。
- **功能设计**：睡眠计时开始后，可选"45分钟后提醒我接觉"，到点发一条本地通知（需要用户授权订阅消息）。
- **技术**：需要 `wx.requestSubscribeMessage`，**需要正式账号**。难度**中**。

### 7.3 排泄与健康场景

**（6）大便性状记录（布里斯托分类简化版）**
- **痛点**：只记"大便"，但医生问起来"是稀的还是硬的"答不上来。
- **功能设计**：大便按钮长按或点开后，选"稀/软/正常/干硬/颗粒"，存一个枚举值。统计页可以看"本周大便性状分布"。
- **技术**：本地存储加枚举字段。难度**低**。

**（7）体温趋势图 + 用药关联**
- **痛点**：发烧时一天量好几次体温，想看看趋势，还想知道"吃了退烧药之后体温有没有降"。
- **功能设计**：体温记录在统计页单独出一个折线图，用药记录用竖线标记在同一时间轴上。
- **技术**：需要自己画 canvas 折线图，或引入轻量图表库。难度**中**。

**（8）尿布库存提醒**
- **痛点**：换尿布时顺手记一笔，但不知道家里尿不湿还剩多少。
- **功能设计**：设置页填"当前库存"，每次记"换尿布"自动减1，低于阈值时首页提示"尿不湿快用完了"。
- **技术**：本地存储加一个计数器。难度**低**。

### 7.4 家庭协作场景（不依赖云开发）

**（9）本地"交接班"备注**
- **痛点**：妈妈去上班，奶奶在家带娃，交接时口头说不清楚"上次几点喂的、吃了多少"。
- **功能设计**：首页加一个"交接班"按钮，生成一张今日记录摘要图（用 canvas 画成长图），可以直接保存发到家庭群。
- **技术**：`wx.canvasToTempFilePath` + `wx.saveImageToPhotosAlbum`。难度**中**，但不需要云开发。

**（10）多宝宝数据隔离 + 快速切换优化**
- **痛点**：已经有多宝宝管理，但切换时可能要进设置页点好几下。双胞胎家庭喂完老大喂老二，切换要快。
- **功能设计**：首页顶部直接放两个宝宝头像 tab，点一下切换，不用进二级页面。
- **技术**：把 `profile` 页的切换逻辑搬到 `index` 页顶部。难度**低**。

### 7.5 数据洞察与"给医生看"

**（11）一键生成"就诊摘要"**
- **痛点**：带宝宝去医院，医生问"最近三天吃了多少、拉了几次、体温怎么样"，家长翻记录翻半天。
- **功能设计**：选一个时间范围（今天/近3天/近7天），自动生成一张结构化摘要图：喂养次数、总奶量、排便次数、体温最高值、用药记录。直接给医生看。
- **技术**：canvas 绘制，或生成一段格式化文本复制到剪贴板。难度**中**。

**（12）异常提醒**
- **痛点**：家长不一定知道"今天排便次数明显偏少"或"奶量比平时低很多"。
- **功能设计**：本地做一个简单的阈值判断，比如"今日排便0次且已过18小时""今日总奶量低于近7天均值的60%"，首页弹一条温和提示。
- **技术**：纯本地计算，不需要后端。难度**低**。

### 7.6 夜间模式补充细节

**（13）夜间模式不只是换色，还要"防刺眼"**
- **功能设计**：夜间模式下，背景用深灰（不是纯黑），文字用暖白（不是纯白），按钮对比度降低，避免半夜看屏幕刺眼。可以加一个"夜间模式自动开启"的时间段设置（比如 22:00-06:00）。
- **技术**：`app.wxss` 用 CSS 变量，各页面适配。难度**低**。

### 7.7 扩展功能优先级建议

| 优先级   | 功能                                 | 理由                                     |
| -------- | ------------------------------------ | ---------------------------------------- |
| **先做** | 左右侧哺乳切换、奶瓶剩余量、大便性状 | 纯本地、难度低、对记录准确性提升明显     |
| **先做** | 首页多宝宝快速切换                   | 体验提升大，改动小                       |
| **先做** | 夜间模式                             | 成本低，夜奶场景刚需                     |
| **再做** | 交接班摘要图、就诊摘要               | 不需要云开发，但需要 canvas 绘制，价值高 |
| **再做** | 睡眠质量标记、异常提醒               | 纯本地，锦上添花                         |
| **后做** | 体温趋势图、接觉提醒                 | 需要图表或订阅消息，依赖正式账号         |
| **最后** | 云端同步、语音记录                   | 架构级改动，建议单独排期                 |

---

## 八、建议的开发顺序（整合版）

综合原有 Backlog 和新增扩展方案，建议按以下顺序推进：

1. **夜间模式**（低成本、体验提升明显）
2. **左右侧哺乳切换 + 奶瓶剩余量 + 大便性状**（记录准确性提升，纯本地）
3. **首页多宝宝快速切换**（体验优化）
4. **定时提醒**（价值最高，但要先解决消息模板审核）
5. **生长曲线 → 数据导出**（"看数据"相关，可一起做）
6. **交接班摘要图 + 就诊摘要**（canvas 绘制，价值高）
7. **睡眠质量标记 + 异常提醒**（纯本地，锦上添花）
8. **疫苗接种日历**

---

*文档版本：v1.1*
*更新日期：2026-09-23*
*更新内容：新增「七、功能扩展方案」章节，补充 13 项扩展功能；新增「八、建议的开发顺序（整合版）」；调整「一、项目简介」中的引导语。*
